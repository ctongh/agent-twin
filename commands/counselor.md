---
description: Start a structured conversation that generates data for the analysis pipeline (questionnaire mode for first-time users, companion mode for returning users)
---

# /counselor

Run a guided conversation that produces a session suitable for `/run_pipeline`. Detects first-time vs returning users automatically and adapts.

To execute, invoke the `counselor` skill via the Skill tool. See `skills/counselor/SKILL.md` for the full specification.
