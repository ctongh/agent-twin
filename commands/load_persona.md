---
description: Silently load the compiled behavior brief into this session so responses adapt to who the user is
---

# /load_persona

Silently load `behavior_brief.md` into the current session so future responses adapt. The skill does not print the brief.

To execute, invoke the `load_persona` skill via the Skill tool. See `skills/load_persona/SKILL.md` for the full specification.
