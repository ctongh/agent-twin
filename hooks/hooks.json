{
  "hooks": {
    "Stop": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "command -v python >/dev/null 2>&1 && python \"${CLAUDE_PLUGIN_ROOT}/scripts/autosave_session.py\" || command -v py >/dev/null 2>&1 && py \"${CLAUDE_PLUGIN_ROOT}/scripts/autosave_session.py\" || true",
            "async": true,
            "timeout": 30
          }
        ]
      }
    ]
  }
}
